# Core Review 改善任務報告

## 已完成改善任務

| 改善任務 | 優先級 | 預期效果 | 狀態 |
|---|---|---|---|
| 為 Modbus TCP/RTU/UDP connector 的 `connected` / `persistentMode` 增加同步保護（RWMutex） | 高 | 降低併發讀寫造成 data race 與狀態競爭風險 | 已完成 |
| 修正 Reconnect/ensureConnection/afterOperation 的連線狀態更新路徑，統一走同步方法 | 高 | 提升重連與短連接切換時的狀態一致性 | 已完成 |
| 修正 RTU/UDP 讀寫時臨時 TCP connector 的模式設定（固定為 persistent） | 高 | 避免臨時物件錯誤關閉共享連線，降低非預期斷線風險 | 已完成 |
| 壓力測試改為使用同步狀態設定方法 | 中 | 減少測試內部非同步寫入造成的誤判與不穩定 | 已完成 |

## 待評估改善任務

| 改善任務 | 優先級 | 預期效果 | 狀態 |
|---|---|---|---|
| PollNow 流程補齊與自動輪詢一致的熔斷器回報行為 | 中 | 讓手動輪詢與排程輪詢的保護策略一致，提升故障隔離一致性 | 待評估 |
| 調整 golangci-lint 版本或 `.golangci.yml` 設定版本相容性 | 中 | 讓 lint 能在 CI/本機穩定執行，維持靜態檢查品質門檻 | 待處理 |

## 驗證摘要
- `go test ./internal/datalink/connector/adapters`：通過
- `go test -race ./internal/datalink/connector/adapters -run TestModbusTCPConnector_StressTest`：通過
- `go test ./...`：通過
- `golangci-lint run ./...`：目前因設定版本相容性問題失敗（非本次重構邏輯錯誤）
