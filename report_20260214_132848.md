# Core Review 改善任務報告

## 本次修正（已完成）

### 1) 修正 CheckReadiness 靜默失敗
- **檔案**: `internal/datalink/device/service_status.go`
- **優先級**: P0
- **改善內容**:
  - `json.Marshal` 失敗時改為回傳錯誤。
  - `repo.Update` 失敗時改為回傳錯誤。
- **預期效果**:
  - 避免就緒狀態寫入失敗卻回傳成功。
  - 降低資料狀態不一致風險並提升可觀測性。

### 2) 修正連線測試結果寫入靜默失敗
- **檔案**: `internal/datalink/device/service_connection_testing.go`
- **優先級**: P0
- **改善內容**:
  - 新增 `recordTestResult`，統一處理測試結果寫入。
  - 寫入測試結果失敗時不再吞錯，改為回傳帶上下文錯誤。
  - `TestConnectionWithResult` 在寫入失敗時回傳錯誤。
- **預期效果**:
  - 連線測試結果可追蹤且錯誤可見。
  - 降低「測試失敗但狀態未記錄」造成的排障成本。

### 3) 新增保護測試
- **檔案**: `internal/datalink/device/service_refactor_test.go`
- **優先級**: P0
- **改善內容**:
  - `TestCheckReadinessReturnsErrorWhenUpdateFails`
  - `TestTestConnectionReturnsErrorWhenResultUpdateFails`
- **預期效果**:
  - 防止未來重構再次引入靜默失敗。
  - 讓關鍵錯誤處理路徑有自動化回歸保護。

## 後續建議（未實作）

### A) 協議驗證策略化
- **檔案**: `internal/datalink/device/service_validation.go`
- **優先級**: P1
- **預期效果**: 降低硬編碼分支，提升新協議擴充性。

### B) 清理未使用欄位或補註解
- **檔案**: `internal/datalink/device/service.go` (`mu`)
- **優先級**: P2
- **預期效果**: 降低維護成本與理解負擔。
