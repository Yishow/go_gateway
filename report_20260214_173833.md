# Core Review 改善任務報告（最終）

## 範圍
- 審查 commit：`3f646ce`、`7334125`
- 聚焦：設定載入路徑的穩定性與錯誤處理一致性

## 改善任務

### 1) 修正 GIN_MODE 環境變數設定錯誤未處理
- 優先級：P1（High）
- 檔案：`internal/config/config.go`
- 問題：`os.Setenv("GIN_MODE", ginMode)` 的錯誤未處理。
- 措施：在 `Load()` 中改為顯式檢查錯誤並回傳 `fmt.Errorf(...%w...)`。
- 預期效果：在受限執行環境下可及早暴露設定失敗，避免隱性配置異常。
- 狀態：✅ 已完成

### 2) 修正 .env 解析錯誤造成行為回歸風險
- 優先級：P1（High）
- 檔案：`internal/config/config.go`
- 問題：先前版本忽略 `.env` 解析錯誤；改動後若直接回錯會造成舊環境啟動回歸。
- 措施：保留相容行為：當 `.env` 非不存在類型錯誤時，退回系統環境變數載入，不中斷 `Load()`。
- 預期效果：避免因 `.env` 格式問題導致服務無法啟動，維持既有容錯行為。
- 狀態：✅ 已完成

### 3) 補上回歸測試覆蓋 `.env` 格式錯誤情境
- 優先級：P2（Medium）
- 檔案：`internal/config/load_test.go`
- 問題：缺少 `.env` 格式異常時的行為測試。
- 措施：新增 `TestLoad_IgnoresMalformedDotEnv`，驗證 Load 不會因 malformed `.env` 回錯。
- 預期效果：鎖定設定載入相容性，防止未來重構再度引入啟動回歸。
- 狀態：✅ 已完成

## 驗證
- `go test ./internal/config` ✅
- `go test ./...` ✅
- `golangci-lint run ./...`（針對本次修正檔案關聯問題已清除）✅
