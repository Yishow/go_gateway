<!--
The first group is an independently completable immediate legacy-deletion
batch. C release-polish tasks remain separate and do not add legacy telemetry,
compatibility, database changes, or Modbus Share concurrency behavior.
-->

## 1. Immediate legacy deletion batch

- [x] 1.1 Deliver **Decision 1: Execute immediate legacy deletion as an independently bounded batch**, the **Canonical studio route and non-target boundaries**, and the **Immediate legacy studio deletion is bounded and complete** contracts by producing a reviewed pre-delete inventory of `/studio` route registrations, direct/lazy imports, chunks, assets, obsolete tests/docs references, preserved-surface dependencies, owner decision, and deletion list. Verify inventory content review proves each deletion candidate is legacy-only and records every uncertain file as retained.
- [x] 1.2 [TDD-Red/Green/Refactor] Deliver **Decision 3: Remove only proven legacy-only frontend graph members** and the **Coexisting v2 workbench route** contract by removing the dedicated `/studio` route registration, handler/tombstone, proven legacy-only imports/chunks/assets, obsolete legacy tests, and obsolete legacy docs references while retaining shared and preserved-surface consumers. Verify source/import reachability, route-table inspection, chunk manifest inspection, and frontend/backend focused tests prove no dedicated legacy registration or legacy mount remains.
- [x] 1.3 [TDD-Red/Green/Refactor] Deliver the **Guided workflow**, **Decision 2: Make `/studio` exactly follow the existing generic unknown-route policy**, the **Default v2 entry route**, and **Legacy landing paths converge on /studio/v2** contracts: after deletion `/studio` and an arbitrary unknown path produce the same response/navigation, status/fallback, error boundary, and network asset pattern; generic datalink landings retain their existing v2 entry behavior without recreating the legacy workspace. Verify side-by-side route/browser tests and negative assertions for `/studio`-specific redirect, fallback, handler, tombstone, or legacy chunk.
- [x] 1.4 Verify **Existing route product semantics remain unchanged during delivery refactoring**, **Embedded browser smoke proves the full asset graph is loadable**, and **Final removal preserves non-target embedded delivery**: `/studio/v2`, `/studio/runtime`, `/test`, and `/gateway/*` retain route identity and required assets, while `/studio` has no legacy markup/chunk/asset. Verify fresh static-tree checks, browser network smoke, route identity tests, and missing-asset failures.
- [ ] 1.5 Deliver **Decision 4: Use a dedicated Git rollback anchor with explicit recovery limits** and the **Git rollback and recovery boundaries are explicit** contract by creating one deletion commit that references the inventory, deletion list, verification evidence, revert command, rebuild command, and redeploy target. Verify a clean revert/rebuild/redeploy smoke and document that Git restores tracked source/docs but not deployment/runtime data, external database contents, device state, or browser bookmarks.
- [x] 1.6 Deliver **Legacy deletion is independent of unrelated lifecycle gates** by allowing the approved deletion batch to proceed without telemetry, usage windows, deprecation phases, query migration, redirect windows, database changes, or A/B acceptance while explicitly leaving Modbus Share lifecycle/CAS/concurrency/ownership requirements unchanged. Verify a gate fixture with absent unrelated evidence that passes only the legacy deletion checks and does not claim Modbus Share acceptance.

## 2. C1 typed errors and safe i18n

- [ ] 2.1 [P] [TDD-Red/Green/Refactor] Deliver **Decision 5: Keep C release polish independent from legacy deletion and Modbus Share**, **Typed safe errors are mapped to v2 i18n**, **V2 error presentation separates operator copy from diagnostics**, **Preview error diagnostics are separated**, and **Runtime error envelope is stable**: preview, runtime snapshot/stream, readiness, and activation failures return `error.code`, `error.message`, `error.retryable`, and opaque `error.request_id`; known codes map to en and zh-TW safe copy while raw exception details stay outside the DOM. Verify focused Go handler/runtime contract tests and frontend error-code tests.
- [ ] 2.2 [P] Complete the **i18n namespace for v2** contract so typed errors, retry actions, accessibility names, Share states, and CommitProgress states resolve through the existing `workbench-v2` en/zh-TW namespaces without hardcoded user-facing strings. Verify locale-key parity, missing-key checks, and rendered-copy tests in `frontend/tests/unit/workbench-v2`.

## 3. C2 preview and runtime truthfulness

- [ ] 3.1 [P] [TDD-Red/Green/Refactor] Deliver **Step 3 preview and SSE states are truthful** and **Live preview**: expose `idle`, `connecting`, `live`, `reconnecting`, `degraded`, `error`, and `stale`, use 1/2/4/8/16/30-second bounded retry, preserve last-success timestamps, show ARIA state, and never substitute local/mock success after server failure. Verify initial failure, disconnect, retry exhaustion, recovery, and no-silent-fallback assertions in Step 3 focused tests.
- [ ] 3.2 [P] Extend **Runtime dashboard layers snapshot, live stream, and degraded fallback** and **Post-setup runtime route shows truthful empty and degraded states** to consume typed codes, show waiting/reconnecting/degraded/error states, and stop live tone after stream loss without changing Modbus Share lifecycle semantics. Verify runtime dashboard state tests, runtime stream handler tests, `/studio/runtime` route tests, and **Runtime error envelope is stable** failure-code assertions.

## 4. C3 accessible controls and progress

- [ ] 4.1 [P] [TDD-Red/Green/Refactor] Deliver **Share and CommitProgress presentation is accessible and single-flight** and **Share toggle and CommitProgress use truthful accessible states**: localized accessible name, `aria-pressed`, visible focus, Enter/Space, pending disabled, one UI request per gesture, and actual pending/success/failed/skipped results with safe retry copy. Verify keyboard/pointer double-submit tests and failed-row assertions; do not add backend CAS or concurrency behavior.
- [ ] 4.2 [P] Complete **Accessible and responsive operator workspace** and **Commit progress reports actual result states** across supported v2 controls. Verify Testing Library accessibility checks, locale-key parity, responsive assertions, and proof that failed rows never render fixed 200 success.

## 5. Formal documentation and final acceptance

- [ ] 5.1 [P] Deliver **Decision 6: Make route/docs/inventory changes evidence-producing**, **Documentation and inventory delivery is traceable**, **Auto-generated API documentation**, and **API registry and release documentation match generated contract**: regenerate Swagger, align API registry/release note/runtime/inventory docs with immediate deletion, generic unknown-route equivalence, preserved routes, pre-delete inventory, and Git rollback limitations; every modified inventory file receives a `studio_inventory_changelog` entry. Verify Swagger generation, registry/release review, changelog.sqlite readback, and no unrelated historical Swagger/data-migration claim.
- [ ] 5.2 Consolidate **Observable behavior**, **Interface / data shape**, **Failure modes**, **Acceptance criteria**, and **Scope boundaries** into the release evidence packet: include deletion inventory, route/handler/tombstone absence, unknown-route comparison, preserved-surface smoke, deletion commit/revert evidence, source/data/bookmark rollback limits, and independent C polish results. Verify content review against every modified requirement and confirm no secrets or raw diagnostics.
- [ ] 5.3 Execute the complete **Acceptance criteria** using focused frontend/backend tests, frontend lint/build, line gate, OpenAPI generation, route/browser/embedded smoke, `spectra analyze`, `spectra validate`, and `git diff --check` restricted to this change. Classify dynamic instability, GUI, external service, PLC, and field acceptance separately; do not report them as source/test pass or Modbus Share acceptance.
