# Studio v2 資料庫流程設計

## Context

以 evidence.md 的固定版本來源為依據。上一輪下載包只是未合併的參考，不是 main 已有的修正。既有規格已要求真實資料與版本保護，但第 4 步規格另保留了示範動畫成功的要求，必須一起收斂。文件相互矛盾不代表舊示範行為可以恢復。

以下設計是目標契約；初稿文件階段後已獲授權開始階段 B，實際進度與驗證見 tasks.md／validation.md，不能把整份設計當成已完成現況。

## Goals / Non-Goals

**In scope**：第 4 步資料庫準備、記錄方案與設備／測量項目／連線的關聯、設定儲存一致性、建表／試寫結果、欄位編輯與預覽保護、文案與可操作性。

**Out of scope**：新增資料庫驅動、改寫 PLC 或採集排程、重寫 Modbus Share 啟用保護、增加舊 Studio 入口、擴建全產品報表、部署及現場寫入。真實測試只使用可丟棄的測試資料庫。

## Decisions

### 1. 遵循已完成的安全封鎖

`fix-studio-v2-database-result-truthfulness` 唯一負責記錄方案空入口的固定 501、能力／安全錯誤與 Step 4 進度／完成卡。本案開始連線／方案整合前，需驗證該案 1.1–1.8 已完成；不再次實作安全封鎖。一般建表在該階段保持可用，後續由 `implement-studio-v2-verified-schema-setup` 遷移至明確確認 gate；相容不代表保留無 token mutation。

### 2. 目標只能來自已儲存資料

瀏覽器送出已儲存的連線編號及預期版本，後端驗證工作區可用範圍並解析真正種類與憑證；不相信瀏覽器自行填的種類來選擇建表語法。`schema_dbtarget_models.go`、dbtarget repository 及必要的本地 migration 負責保存 connector identity revision 與 server target resolver；身份／憑證變更必須遞增 identity revision，健康檢查不得代替 identity revision。記錄方案建立、預覽、建表與試寫共用同一個目標選取結果，並以兩種 kind、foreign／unknown connector 及 dialect spoof 測試驗證。

不再使用 `default-sqlite`、`meas-primary` 或第一台設備當補值。從已儲存的測量項目讀取每一筆設備歸屬；原始點位編號不能未經確認就當成測量項目編號。沒有合法成員時停止建立，指出缺少的設定。

多方案時必須選方案；只有唯一且符合目前範圍的方案才可自動選取。切換方案／設備／連線會清除該範圍的預覽和試寫顯示。清單讀取失敗與空清單必須分開，不能把載入失敗當成可以重新建立。

重用未改變的已存連線，不因密碼被遮蔽而要求重填。密碼原字串不得 trim；未變更、明確清除及重新提供必須能區分。身分改變後不沿用前一端點密碼。憑證、連線密碼與敏感路徑不寫入普通畫面、記錄或預覽簽章。

### 3. 真實欄位與建表建議分開

優先沿用 `dbTargetAPI.listTables(id)` 所對應的服務讀取能力，由服務層與查詢 hook 統一處理，不在畫面元件散落呼叫。查詢資料以已存連線身分版本、資料庫、分類及資料表分開；改變任何一項就使舊結果失效。

明確顯示尚未查詢、讀取中、表存在、表不存在、權限不足或查詢失敗。既有表查詢失敗時不能使用範例欄位補位。新表的欄位是待確認建議；既有配對若仍相容就保留，缺少或有歧義就標示待修復，不能循環使用欄位或以名稱相似直接確認。

本案 2.3 只負責配對與顯示；metadata provider 由 `implement-studio-v2-verified-schema-setup` 提供。欄位衝突沿用 `workspace-database-row-groups`：同一群組且有合法資料列識別方式的共用欄位可以存在；跨群組或缺少識別方式仍阻擋。不把此次 UI 整理變成資料寫入語意變更。

### 4. 一次儲存要有一致的結果

此階段以已存設定與固定測試資料驗證原子儲存，不依賴尚未完成的 metadata／自動配對介面。先故意使第二段本地儲存失敗，確認連線／分組及欄位對應／工作區參照的實際行為。同一個本地設定資料庫的變更使用同一筆交易與版本檢查，connector identity revision 與設定 revision 一起保存：一起成功或一起失敗。尚未儲存完成的設定不取得可執行資格。

外部建表不假裝能與本地設定跨資料庫一起還原。改為先保存操作編號與目標版本，再記錄實際結果；結果不確定時顯示需要核對，禁止假成功和盲目重試。啟用設備前重新取得最新後端可執行結果，保留目前的工作區／設定版本與 readiness token 檢查。

### 5. 消費已驗證的建表結果

`implement-studio-v2-verified-schema-setup` 唯一負責 metadata、schema preview 的 scope／revision／digest／期限、持久 token／operation、原子 claim、operation 查詢與所有公開建表入口的確認保護。本案 3.3 沿用該機制，擴充 `test_write` 操作種類、試寫預覽及查詢結果，不另建第二套 token 或 ledger；建表 token 不得用於試寫。該案完成 3.1–3.10 後，本案 2.3 才使用其欄位，3.3 才在已核對 schema 上實作試寫。

一般 GenerateSchema 及 EnsureWorkspaceSchema 的遷移亦由該案負責：前者保留安全預覽與確認後建表；後者僅核對已存在結構，activation 不建表。舊預覽缺少必要保護欄位時必須重建，不可補猜版本。

### 6. 試寫、讀回與清理各自有證據

確認後才試寫，使用後端保存的操作編號與目標範圍；在目標端以可持久辨識的測試記錄身分避免重複。對相同操作重試或服務重新啟動後，先查既有狀態；無法確認有沒有寫入就停止，不再插入一筆。

本案 3.3 負責以下試寫契約；路徑皆相對於既有 datalink API base：

- 新增 `POST /studio-v2/workspace/recording-plans/test-write-preview`，要求已存 `plan_id`、`connector_id` 與預期 workspace／plan／connector revisions。後端核對 scope 及已驗證 schema，保存 `test_write` 種類、測試內容 digest、版本與期限，回傳預覽內容、token 及固定 operation_id；預覽不插入或清理目標資料。
- 既有 `POST /studio-v2/workspace/recording-plans/test-write` 改要求 `plan_id`、token、operation_id 及上述預期 revisions。只有本工作區、正確操作種類、scope／內容／版本仍相符且未過期的首次確認能取得執行權；缺欄回 400、foreign／unknown 回安全 404、錯誤種類回 422、stale／expired 回 409，均零目標 mutation。舊版僅送 plan_id 不可直接試寫。
- 沿用 `GET /studio-v2/workspace/database-operations/:operation_id`，由本案擴充試寫結果與操作種類，foreign／unknown 均為安全 404。同一操作 running 回 202；已有保存結果回 200（包含未確認結果），不得因重送或 token 後續到期再執行；另一操作佔用同 scope 回 409。前端遇回覆遺失只查同一 operation_id，不新發 preview 迴避鎖定。

回讀比較實際測試內容，不只比較編號。沿用並收斂 `written_verified`、`written_unverified`、`failed`；另以 `unknown` 表示無法確認是否寫入。前端遇到未知狀態也不得顯示成功。`table` 與時間只能取真實結果，沒有資料就顯示尚未確認。

新增獨立的 `cleanup_status`（`not_attempted`、`cleaned`、`failed`、`unknown`），清理僅能處理該操作擁有的測試記錄；不得刪除正式資料。已清理後仍保留操作收據，讓重試不會重寫。缺少讀取或刪除權限時分別顯示已寫入但未驗證、未清理；不能統稱成功且清理乾淨。

以上為擬議的結果契約；實作時同步更新 Go 回覆、TypeScript 型別、服務、UI 與受影響 API 文件。

### 7. 讓輸入與預覽不互相搗亂

每列分開保存草稿、已儲存值與儲存狀態。其他列刷新不得覆蓋目前輸入；同一列的遠端變更必須提示衝突，保留本地文字並提供重新載入或明確重新送出。刪除列後清理其草稿與進行中結果。

Enter 僅走一條提交途徑，中文組字時不送出；連續 Enter、blur 或快速雙擊最多產生一次同內容要求。送出中保留操作身分，不因短暫切換畫面而取消防重複。

預覽／建表回覆必須核對產生時的範圍及版本。過期結果不能更新新設定畫面。忽略舊答案不等於後端已停止操作；沒有取消確認就顯示仍待核對。

唯讀規則涵蓋全部修改動作，包括建表、套用預覽、試寫、連線切換及批量啟停。完成後需明確返回編輯才可修改，修改使相關檢查失效。

### 8. 第 4 步用四個問題引導

1. **資料要存到哪裡？** 顯示設備／方案範圍、資料庫連線、已儲存狀態與測試連線結果。
2. **使用現成資料表，還是建立新的？** 路線明確；「系統安排記錄表」與「自行選表配欄位」不能同時啟用成兩份重複輸出。
3. **哪個資料放哪個欄位？** 使用現成表時顯示來源、目的欄位、檢查、儲存狀態及是否記錄；系統安排記錄表時顯示成員與預期結構，不強迫填手動欄位。詳細位址／型態／分組收在進階區。
4. **現在可以開始了嗎？** 依實際缺項指出下一步；分開顯示設定、建表、試寫、清理、設備運作與最近寫入，未確認不畫成成功。

搜尋與「只看有問題」只影響顯示；批量操作明確標示作用範圍並顯示筆數，不能暗中修改篩選外資料。表格擁有自己的橫向捲動區域，控制項有名稱、焦點可見，錯誤可由鍵盤定位。驗收尺寸訂為 390、768、1440 CSS px；這是設計檢查條件，不是已測量結果。

沿用既有外觀與元件，不引入新的視覺套件。畫面文字進既有 en／zh-TW 翻譯檔，例如「預覽會建立什麼」「哪些資料一起存成一筆」。沒有外部視覺設計來源，不建立虛構設計稿或像素驗收證據。

啟用進度由現有後端實際結果產生，不播放固定步數就宣布完成。部分設備成功、部分失敗要分開呈現；原本已運作的設備不代表這次資料庫修改成功。返回頁面重取真實狀態，不重播假動作。

## Implementation Contract

本節取代初稿重疊待辦的分工，不取消產品需求，不把移交視為完成。所有 `[ ]` 保持未完成；本案沒有已完成待辦或 touched provenance 需要改寫。

| 階段 | 唯一負責 change／待辦 | 可觀察交付與進入條件 |
| --- | --- | --- |
| A | `fix-studio-v2-database-result-truthfulness` 1.1–1.8 | 無前置；安全 501、真實能力／錯誤與後端完成證據，含 router／UI 回歸 |
| B | 本案 1.2、1.3、2.1、2.2、2.4 | A 完成後；獨立列草稿、readonly、已存目標／成員與版本、第二段失敗全回滾；不執行真實建表／試寫 |
| C | `implement-studio-v2-verified-schema-setup` 3.1–3.10 | B 完成後；真實 metadata、持久 token／operation、確認後建表與旁路拒絕；試寫仍封鎖 |
| D | 本案 2.3、3.3、3.4、4.1、4.3 | C 完成後；合法配對、真實試寫／回讀／清理與四問題引導，完成整體驗收 |

跨案條件是工作項目層級 gate，不要求本案整案完成才開始 C，避免循環；每次切案前由主代理核對指定待辦及其測試證據。`[after: ...]` 僅表示同案依賴，不用虛構的跨案編號交給 CLI 解析。共用的 recordingplan service、Step4Database、SchemaSetupSection、語系與 API 文件依 A→B→C→D 串行修改，每批維持可建置。

**Supersedes（2026-09-15 review）**：初稿 1.1（安全封鎖）移交 A；3.1、3.2（預覽／建表）移交 C；4.2（後端完成判定）移交 A、由 D 的 4.3 回歸驗證。這四項從本案可執行清單移除，原始描述可由 Git 基準 `569da9f` 查閱，沒有標成 `[x]`。原 C2／C4／C5 未發布附件中的工作，現僅依已在 repo 的 B／D 規格執行；未讀附件不是需求來源。

範圍／錯誤／驗收以 Decisions 2–8、各 delta scenario 及 tasks 的具名測試為準。未知外部操作不重放、不取得 readiness；保留既有 workspace/settings revision、readiness token 與 Modbus Share 保護，不重寫 runtime。產品完成後各案仍須獨立 verify/review，依 A→C→本案的順序 archive；本次文件 review 不 archive。

## Risks / Trade-offs

- 停止假成功可能讓原本看似可用的按鈕變成未開放；這是避免誤導的必要改變，不以成功回覆維持表面相容。
- 真實欄位查詢需要權限且可能較慢；載入／失敗必須可見，不用範例取代。尚未量測速度，不承諾加速比例。
- 不同資料庫的建表還原、時間與試寫語法不同；依已驗證能力開放，未知種類維持封閉，不宣稱全面支援。
- 本地儲存與外部資料庫無法一概保證一起還原；留下可核對操作狀態，接受需要人工確認的結果。
- 任務分組是交付安排，不是工期估算。若實作超出單次可驗證範圍，先更新任務與依賴再拆批，不跳過測試。

## Migration Plan

先加入安全失敗與互動保護，再擴充身分／版本／操作記錄與相容讀取，完成目標與成員移轉後才開放真實操作。新增欄位不得把舊資料自動標為已驗證。每批產品程式變更先寫會失敗的測試，修正後再跑測試與建置。

既有 row-group 語意維持相容；一般建表遷移至 C 的確認契約，tokenless mutation 不屬於保留範圍。若需要改 API，先保留原安全讀取路徑、成套更新呼叫端，再移除過時寫入行為。最後通過完整測試與可丟棄資料庫驗證後才談部署，不能拿正式 PLC／資料庫當測試環境。

回復程式版本不會撤銷已建立的表或已寫入資料；交接需記錄目標版本及實際變更。無法安全核對時停用新的寫入操作，保留設定與證據，不執行破壞性還原。

## Open Questions

沒有需要先由使用者補答才能保存草案的問題。仍需實作前驗證：CLI 正式文件檢查、各目標資料庫實際能力、本地第二段儲存失敗的行為及既有資料相容性。這些是明確的檢查工作，不預先宣稱通過。
