## 1. Predecessor reconciliation and fixed-point scope

> Gate evidence ledger: [release-evidence.md](release-evidence.md). A task remains unchecked when its exact action-time gate is blocked or the required phase evidence was not recorded.

- [x] 1.1 Reconcile `fix-protocol-address-adaptation-v2` before implementation and freeze the design decision **Reconcile the predecessor and freeze two independent Git inventories**; satisfy **Staged and unstaged release baselines are checked independently** by recording one fixed point, active-change status, `git diff --cached --name-status`, `git diff --name-status`, and `git ls-files --others --exclude-standard` as separate inventories; verify the recorded inventories identify predecessor, A-class, and unrelated dirty paths without modifying any outside-owned file.
- [x] 1.2 Deliver **Release-hardening scope is auditable and bounded** and **Enforce scope hygiene and the legacy line boundary**: classify forbidden connector/transport/legacy paths, uncalled exports, and unrelated compression for removal or handoff; record the fixed-point count of `frontend/src/pages/datalink/workbench/sourceCanvasModel.ts`; verify forbidden-path inventory, caller search, and `scripts/check_file_lines.sh` report no net increase for that historical file. The final elevated Git Bash run passed for 39 files with the same nine warnings, and `sourceCanvasModel.ts` remained 715 -> 715 with no new file over 500 lines.

## 2. Protocol address contract (TDD)

- [ ] [P] 2.1 [TDD-Red] Define tests for **Protocol-radix source address validation and expansion** and **Use a fail-closed protocol address contract**: cover frontend `X0 + 16 = X10`, decimal `D0 + 16 = D16`, FATEK/Modbus examples, invalid input errors, empty derived points, and backend rule/address validation; verify the focused `frontend/tests/unit/utils/addressParser.test.ts`, `frontend/tests/unit/workbench-v2/sourceRule.test.ts`, and Go source-rule tests fail for the unimplemented assertions. The ledger records the focused tests passing after implementation, but no pre-implementation RED run.
- [x] [P] 2.2 [TDD-Green/Refactor] Implement the protocol-radix and invalid-address behavior across `frontend/src/utils/addressParser.ts`, `frontend/src/features/datalink/workbench-v2/state/sourceRule.ts`, and backend source-rule validation without editing connector adapters or protocol transport; preserve the **Interface and data shape** and **Failure modes** from design; verify focused frontend tests plus `go test` for the source-rule package pass and invalid raw addresses never appear as successful points.

## 3. Aggregate Step 2 readiness and device ownership (TDD)

- [ ] [P] 3.1 [TDD-Red] Add focused scenarios for **Step 2 continuation evaluates every enabled rule** and **Source-rule device ownership is fail closed**: cover one valid and one invalid enabled rule, disabled invalid rule, an unknown/deleted `device_id` only when exposed by a stale API/workspace record, two devices with different protocols, and no `devices[0]`/first-Modbus fallback; verify `frontend/tests/unit/workbench-v2/step2-rule.test.tsx` and the relevant Studio V2 source-rule handler tests fail before implementation. Do not require a List API to bypass `OrderedDeviceIDs`/`ListByDeviceIDs` or enumerate cascaded-away rules. The ledger records the focused tests passing after implementation, but no pre-implementation RED run.
- [x] 3.2 [TDD-Green/Refactor] Implement **Make Step 2 continuation an aggregate rule and ownership gate** so every enabled rule is validated by its own device protocol, issues carry rule/device/address identity, and stale/inconsistent rule persistence or activation is rejected without fallback; preserve the **Observable behavior** contract, keep first-device selection limited to new default-rule construction, and treat normal device deletion as FK/cascade removal of its device-owned source rules. Verify the focused Step 2 and handler tests plus `go test ./internal/api/handlers/... ./internal/datalink/sourcerule/...`; the backend cascade contract path is `internal/api/handlers/studio_v2_workspace_devices_handler_cascade_test.go`, with `internal/datalink/db_sqlite_fk_test.go` proving the per-connection FK contract. Latest focused/package and cascade tests pass with task-specific `GOCACHE`; no first-device fallback or pre-existing orphan cleanup is part of this task.

## 4. SQLite migration safety and rollback

- [ ] [P] 4.1 [TDD-Red] Define **SQLite source-rule migration preserves legacy data and supports safe recovery** tests with a legacy row, non-default existing values, each partial-column state, repeated migration, safe defaults, and a documented down/backup decision; verify `internal/datalink/migrator_test.go` focused cases fail before the migration hardening is present. The ledger records the focused migration tests passing after implementation, but no pre-implementation RED run.
- [x] 4.2 [TDD-Green/Refactor] Implement **Harden SQLite upgrades without rewriting source-rule data** in `internal/datalink/migrator.go` and the existing source-rule migration SQL: inspect columns, add only missing fields, preserve rows/NULLs, and document the operator-confirmed down/backup path without adding PostgreSQL live-support requirements or new Share lifecycle semantics; verify focused migrator tests run twice and assert row equality, safe defaults, partial-upgrade recovery, and rollback documentation review.

## 5. Scope, line, and inventory enforcement

- [x] 5.1 Apply the **Scope boundaries** and final predecessor cleanup: remove only the approved predecessor drift in the owned diff, keep `internal/datalink/connector/adapters/`, `internal/protocol/`, unrelated legacy `/studio`, B-class Share lifecycle, and unrelated dirty work untouched, and keep `sourceCanvasModel.ts` at or below its fixed-point count; verify separate staged/unstaged inventories, exact forbidden-path diff queries, `rg` caller checks for exports, and `git diff --check --` for the resulting path set.

## 6. Release evidence runners and embedded delivery

- [ ] [P] 6.1 Implement **Default-worker full Vitest is the release acceptance mode** and **Cross-platform and environment failures are classified fail closed**: run `npm run test -- --run` with default workers, retain per-run inventory/mode/error evidence, and label single-worker or mixed assertion/timeout/toolchain results `blocked`/`unknown`; verify the frontend stability runner/tests preserve dynamic failure identities and never convert a single-worker pass into acceptance. Final NormalFull Repeat 2 remains blocked: run 1 was 991/985 pass with 6 timeouts in 71.3s; run 2 was 991/982 pass with 9 timeout identities across 8 files in 94.4s; four exact identities overlapped, watchdog stayed false, and the identities drifted. DefaultGroup 45/45, SingleWorker 45/45, and FreshIsolated 10 files/45 tests are diagnosis-only; no `onTaskUpdate` error or orphan Vitest worker was observed.
- [x] [P] 6.2 Implement **Windows embedded EXE build and route smoke are release gates** and **Treat release gates as evidence-producing and fail closed**: execute the complete `scripts/build.ps1` path with `pwsh -NoProfile`, produce `bin/test-ui.exe`, start it in a clean temporary directory, and exercise `/studio/v2`, `/studio`, `/studio/runtime`, `/test`, and the existing experimental route with asset checks; satisfy **Release gates produce complete fail-closed evidence** by recording command, working directory, mode, exit result, and failure classification; verify build/synchronization/child-process failures leave the gate incomplete and the Playwright embedded-delivery smoke records route and asset outcomes.

## 7. Final gate and handoff

- [ ] 7.1 Satisfy the **Acceptance criteria** with recorded evidence: run frontend lint, default-worker full Vitest, frontend build, `go test ./...`, `go vet ./...`, `golangci-lint run ./...`, line-limit, `git diff --check --`, `spectra analyze harden-studio-v2-release-correctness --json`, `spectra validate harden-studio-v2-release-correctness --strict`, and normal `spectra validate`; verify every required gate is pass or explicitly incomplete with its error, and no Critical/Warning analyzer finding remains. The final P1 focused frontend contracts (3 files / 18 tests), lint/build, post-A `go test ./... -count=1`, `go vet ./...`, and formal 39-file line gate pass; remaining blockers are default-worker Vitest, golangci-lint v1 versus v2 configuration, archived named-change analysis, historical TDD RED evidence, and external/live acceptance.
- [x] 7.2 Keep source evidence separate from physical acceptance in the final handoff: report source/unit/integration/build/route evidence separately from PLC communication, actual database target, GUI, network, and factory-field checks; verify the report states that source gates and embedded smoke do not constitute physical acceptance and that no external gate is marked complete without action-time evidence.
