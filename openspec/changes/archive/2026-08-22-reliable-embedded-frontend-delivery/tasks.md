## 1. 路由載入契約

- [x] 1.1 [P] 在 `tests/unit/app-routing-lazy-load.test.tsx` 先建立 route contract 測試，具體覆蓋「Lazy route loading has one deterministic loading and error contract」的 pending fallback、lazy route resolved content、rejected import error boundary，以及既有 redirect assertion；這是設計中「Decision: Use route-level lazy imports behind one deterministic Suspense boundary」與「Observable behavior」的可重跑驗證，完成後以 `cd frontend; npm test -- --run tests/unit/app-routing-lazy-load.test.tsx` 驗證測試能命中上述四種狀態。
- [x] 1.2 將 `frontend/src/App.tsx` 的重型既有 route module 改為 route-level dynamic imports，讓同一個 deterministic Suspense fallback 包住既有 route tree，並保留相同 component props、route parameters、redirect 與 error-boundary 行為；此 task 必須交付「Existing route product semantics remain unchanged during delivery refactoring」，並以 `cd frontend; npm test -- --run tests/unit/app-routing-lazy-load.test.tsx` 及 `cd frontend; npm run build` 結果驗證「Interface and data shape」。
- [x] 1.3 [P] 依「Decision: Keep vendor chunk groups explicit and stable in Vite output」更新 `frontend/vite.config.ts`，使 React/runtime、Material UI/Emotion、charting、router/i18n/query 與 residual dependency 產生穩定 vendor group，且 application route chunks 保持可追蹤的相對引用；此 task 必須交付「Frontend build emits a complete embedded asset graph」，以 `cd frontend; npm run build`、entry HTML reference review，以及 shell asset-graph case 驗證每個 direct/transitive import 都能找到輸出檔。
- [x] 1.4 [P] 更新 `.gitignore` 並保留 tracked `cmd/test_ui/static/embed-placeholder.txt`，使 generated static children 維持 ignored 而 fresh checkout 在沒有 `frontend/dist` 時仍能匹配 Go embed static pattern；以 `git check-ignore` 檢查 generated sentinel、在 disposable checkout 執行 `go build ./cmd/test_ui`，以及確認無 generated bundle 被追蹤來驗證 fresh-clone contract。

## 2. 跨平台資產同步

- [x] 2.1 [P] 依「Decision: Synchronize generated assets by invariant, not by directory replacement」調整 `scripts/build.ps1`，令成功流程先建立 static、只保留 placeholder、移除 stale generated children，再完整複製 `frontend/dist` 並在缺少 dist 或 copy 失敗時回傳非零；此 task 必須交付「Embedded static synchronization mirrors the complete frontend output」與「Build and synchronization failures are surfaced and fail closed」，以 `tests/shell/embedded-frontend-delivery.sh` 的 `powershell-build-sync` 與 `missing-dist` cases 驗證。
- [x] 2.2 [P] 將 `start.ps1` 的 `Build-Frontend` asset synchronization 對齊同一四步 invariant，保留 placeholder、清除 stale generated assets、完整同步 nested dist tree，並維持 build/copy failure 的 nonzero signal；以 `tests/shell/embedded-frontend-delivery.sh` 的 `powershell-start-sync`、`stale-file-removal` 與 `copy-failure` cases 驗證 observable output。
- [x] 2.3 [P] 將 `start.sh` 的 `build_frontend` asset synchronization 對齊同一四步 invariant，確保 POSIX path 與 PowerShell path 的 target tree、placeholder preservation、stale cleanup 與 failure status 一致；以 `tests/shell/embedded-frontend-delivery.sh` 的 `posix-start-sync` 與 `missing-dist` cases 驗證 parity，並確認其他未授權啟動行為未變更。

## 3. 交付驗證與 smoke

- [x] 3.1 建立 `tests/shell/embedded-frontend-delivery.sh` 的 fixture-driven cases `powershell-build-sync`、`powershell-start-sync`、`posix-start-sync`、`stale-file-removal`、`copy-failure` 與 `missing-dist`；測試必須比較 `frontend/dist` 與 `cmd/test_ui/static` 的完整相對檔案樹、確認 placeholder 保留、確認 stale sentinel 被刪除、確認 copy/dist failure 回傳非零，並依「Decision: Verify the asset graph at source, embedded, and browser boundaries」覆蓋「Failure modes」及「Acceptance criteria」；以 `bash tests/shell/embedded-frontend-delivery.sh` 執行全套 cases。
- [x] 3.2 在 disposable fresh checkout 先保留 tracked placeholder、移除所有 generated frontend assets，再執行 `go build ./cmd/test_ui`；成功結果必須證明 fresh-clone compile 不依賴 dist output，失敗結果必須指向缺少 static match，並以「Scope boundaries」檢查沒有為通過編譯而加入 generated bundle 或修改 unrelated staged files。
- [x] 3.3 由 Playwright spec `frontend/tests/e2e/embedded-frontend-delivery.spec.ts` 擁有 embedded browser smoke；執行 `cd frontend; npm run test:e2e -- tests/e2e/embedded-frontend-delivery.spec.ts` 後，依序開啟 `/studio/v2`、`/studio`、`/studio/runtime`、`/test` 與一個 `/gateway/*` route，等待 lazy imports settle 並記錄所有 JavaScript/CSS/route/vendor request；此 task 必須交付「Embedded browser smoke proves the full asset graph is loadable」，以 network response 非 404、既有 redirect/route identity assertion 與 missing-asset negative case 驗證。
- [x] 3.5 依「Decision: Fail closed when the frontend source is missing」修正 `start.sh` 的 `build_frontend`，使 `frontend` source directory 或必要 build input 缺失時回傳 nonzero 並阻止同次 backend build；在 `tests/shell/embedded-frontend-delivery.sh` 新增 `missing-frontend-source` fixture，移除 source、透過 stub backend compiler marker 執行 build path，驗證錯誤指出 source 且 marker 不存在，並以 `bash tests/shell/embedded-frontend-delivery.sh missing-frontend-source` 重跑。
- [x] 3.6 [P] 依「Decision: Make Playwright webServer startup cross-platform」更新 `frontend/playwright.config.ts` 的 `webServer` command/environment，交付「Playwright webServer startup is cross-platform」，使 Windows 與 POSIX 都能從 `frontend` 直接執行 `npm run test:e2e -- tests/e2e/embedded-frontend-delivery.spec.ts`、等待 declared readiness URL 後進入 route assertions，且不依賴 POSIX-only environment assignment；以該 exact command 在 Windows 與 POSIX（或等價 platform-specific CI jobs）各執行一次並檢查 webServer readiness 驗證。
- [x] 3.7 依「Decision: Enforce historical launcher line-count ceilings」在 final gate 前比較 `start.ps1`、`start.sh` 與各自 pre-change baseline，交付「Historical launcher line counts do not regress」；若修復造成行數增加則在同一檔案內縮減而不改變 asset synchronization/fail-closed contract；執行 `bash scripts/check_file_lines.sh`、baseline comparison 與 `rtk git diff --check`，確認兩檔行數均不超過 baseline 且沒有新增 hard-limit violation。
- [x] 3.4 依設計的「Acceptance criteria」完成 final content review：核對「Observable behavior」、「Interface and data shape」、「Failure modes」與「Scope boundaries」逐項有測試或命令證據，執行 `rtk git diff --check`、`rtk spectra analyze reliable-embedded-frontend-delivery --json` 與 `rtk spectra validate reliable-embedded-frontend-delivery`，並確認 diff 不含 generated assets、lockfile drift、Workbench data behavior、route semantic rewrite 或其他未授權檔案。
