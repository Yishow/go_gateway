## 1. 靜態盤點與環境 capture

- [x] [P] 1.1 完成 environment capture 與 dynamic failure-set 診斷前置盤點：記錄 apply host 的 OS、Node/npm、Vitest 版本、CPU/memory 摘要、background-process snapshot、frontend package script、既有 Vite/Vitest config hash、reporter capability，以及以 rg 定位的 exact candidate paths；unsupported metrics 記為 unavailable，不猜測；驗證目標為 pwsh -NoProfile -Command "rg --files frontend/tests -g '*.test.ts' -g '*.test.tsx'"、Get-FileHash frontend/package.json、Get-FileHash frontend/vitest.config.ts（若檔案存在）與人工確認沒有修改 dependency、timeout、discovery 或 production path。

## 2. Runner 與 evidence contract

- [x] 2.1 實作 The diagnostic runner reproduces and classifies dynamic full-suite failures：建立 scripts/diagnose-frontend-vitest-matrix.ps1 的 Mode、Repeat、OutputPath、GroupFiles 介面，讓 Environment mode 可輸出 schemaVersion 1、runId、settings、discovered counts、dynamic failures、result 與 artifactPaths；驗證目標為 pwsh -NoProfile -File scripts/diagnose-frontend-vitest-matrix.ps1 -Mode Environment，並以 Observable behavior、Interface and data shape、JSON parse 與 content review 確認預設 unique output directory 且沒有固定十個 suite identity。
- [x] 2.2 完成 Decision: 證據 schema 與 classification gate：將 assertion、test-timeout、worker-hang、resource leak、environment、unknown 的判定與 stdout/stderr、exit code、phase、process/heap metrics 寫入 machine-readable evidence；驗證目標為以 synthetic runner outputs 或可重跑 diagnostic records 做 Failure modes 與 classification table review，確認每個非 pass 結果只有一個分類且 unknown 會 blocked。

## 3. Sequential matrix

- [x] 3.1 完成 Decision: 動態 failure-set 與 normal baseline：以既有 frontend command npm --prefix frontend run test -- --run 執行 NormalFull 三次，保存每次 runId、failure file/title、discovered totals、elapsed、test-timeout 與 external-watchdog 欄位；驗證目標為 pwsh -NoProfile -File scripts/diagnose-frontend-vitest-matrix.ps1 -Mode NormalFull -Repeat 3，並人工比對三筆 JSON。
- [x] 3.2 完成 Root-cause diagnosis uses a sequential single-variable matrix 與 Decision: 單變因矩陣與 sequential execution 的 FreshIsolated phase：由 NormalFull 當次 observed failure files 動態產生清單，每檔以 fresh process 重複十次且不共享 mutable state/output；驗證目標為 pwsh -NoProfile -File scripts/diagnose-frontend-vitest-matrix.ps1 -Mode FreshIsolated -Repeat 10，確認 phase 完成後每個 observed file 都有十筆或明確 blocked records，且沒有並行 Vitest process。
- [x] 3.3 依 Decision: 單變因矩陣與 sequential execution 完成 DefaultGroup 與 SingleWorkerGroup：先執行 DefaultGroup，再只切換 worker setting 執行 SingleWorkerGroup，保留相同 observed files、order、isolation、pool 與 command；驗證目標為兩次 sequential mode command 的 JSON settings diff 僅包含 worker 欄位，並確認 single-worker 結果只作 diagnosis signal。

## 4. 假說與分類決策

- [x] 4.1 完成 Decision: Hypothesis falsification table：針對 worker/resource saturation、test-local lifecycle、order/isolation coupling、assertion defect、environment effect 逐一指定可否證的單變因 branch、至少兩筆 comparable observations 與停止條件；驗證目標為 content review 確認每個假說均有 falsification test、supporting evidence 與 consequence，且不把十個 timeout suite 固定為 identity。
- [x] 4.2 完成 The diagnostic runner reproduces and classifies dynamic full-suite failures 的 full classification review：將每個 run 的 raw output、phase timing、process/heap/worker metrics 與 result 交叉檢查，確認 assertion/test-timeout/worker-hang/resource leak/environment/unknown 的 observable boundary；驗證目標為 JSON schema validation、classification table review 與 blocked record assertion。

## 5. Conditional test-only repair

- [x] 5.1 依 Decision: Conditional bounded repair 執行互斥的 RED 或 blocked/N/A 分支：只有至少兩筆 comparable evidence 證實 test-local defect、classification gate 解鎖且 dynamic path 位於 proposal Impact 的 exact 11-path union（或已證實的 frontend/src/setupTests.ts shared cleanup）時，才建立 TDD characterization RED；若 gate 未解鎖、證據不足、分類非 test-local，或 dynamic path 不在 union，則不得造 RED，以 linked blocked record、N/A decision 與 preserved evidence 標記本 task 完成，scope 外 path 必須先停止並執行 spectra-ingest；RED 分支保留原 title、assertion、mock、fixture、route 與 cleanup contract，驗證目標為 matrix record 所列 exact root-relative path 的 pre-change failure output 並連回 root cause。
- [x] 5.2 僅在 5.1 產生 RED 時完成 Proven test-local defects receive bounded contract-preserving repairs：只修改同一 Impact union 內的 evidence-selected test、test-only harness 或有明確 cleanup 證據的 frontend/src/setupTests.ts，實作最小 GREEN repair，不修改 timeout、worker/pool、config、dependency、coverage、skip/retry/exclude 或 production code；若 5.1 為 blocked/N/A，則以 no-source-change/N/A evidence 完成，不修改任何 source、test、harness 或 setup path；驗證目標為同一 matrix-selected focused command npm --prefix frontend run test -- --run GREEN（僅 RED 分支）、title/assertion/mock/fixture/route/cleanup/discovery parity review、make check-lines 或 repo-supported equivalent（Git Bash scripts/check_file_lines.sh）與 exact diff scope review，scope 外 path 必須先停止並執行 spectra-ingest。
- [x] 5.3 完成 Conditional bounded repair 的 branch closure：記錄 5.2 GREEN 的 chosen repair，或記錄 5.1/5.2 blocked/no-code/N/A branch、linked matrix evidence、scope gate decision 與未修改 source 的證明；拒絕任何 worker/config/timeout workaround，並確認 dynamic evidence 沒有導向未經 spectra-ingest 核准的 path。驗證目標為 content review 確認 tasks 產出單一 branch closure handoff 且 git diff 僅含允許的 diagnosis artifact 或已證實 test-only repair。

## 6. Stability gate 與回滾

- [x] 6.1 完成 Stability acceptance separates focused correctness from full-suite evidence：若 5.3 選擇 blocked/no-code branch，不重跑額外三次 stability full suite、不 claim stability，以既有 NormalFull baseline、blocked evidence、no-source-change/N/A handoff 完成；只有 chosen bounded repair 才依序執行 affected focused repeat、relevant DefaultGroup、NormalFull 三次、npm --prefix frontend run lint、npm --prefix frontend run build 與 make check-lines 或 repo-supported equivalent（Git Bash scripts/check_file_lines.sh）；驗證目標為對應 branch 的 Acceptance criteria command outputs、修復分支三次 normal run 無 classified timeout、focused/group behavior preserved，且不把 focused pass 當成 full-suite acceptance。
- [x] 6.2 完成 Matrix process handling is isolated and resource-safe 與 Decision: Process/resource safety and rollback：確認 runner 只追蹤自己建立的 child process tree、unique output directory、external watchdog 與 Vitest test-timeout 分離，unsafe termination 會 blocked 且不 global kill；驗證目標為 process-safety review、owned PID cleanup evidence、temporary output cleanup assertion，以及以 exact evidence-selected paths 的 rollback rehearsal。
- [x] 6.3 完成 final audit 與 durable handoff：重讀 proposal.md、design.md、specs/frontend-test-suite-stability/spec.md、tasks.md，核對 requirements、Decision headings、Execution Decisions、Scope boundaries、blocked branch、TDD evidence、matrix records、focused/full-suite/lint/build/line outputs 與 known risks；驗證目標為 spectra analyze diagnose-frontend-full-suite-timeouts --json、spectra validate diagnose-frontend-full-suite-timeouts、git diff --check、make check-lines 或 repo-supported equivalent（Git Bash scripts/check_file_lines.sh）與 exact path/status audit，確認沒有 config、dependency、production 或 unrelated dirty path 變更，且 scope 外 dynamic path 已停止並經 spectra-ingest。
