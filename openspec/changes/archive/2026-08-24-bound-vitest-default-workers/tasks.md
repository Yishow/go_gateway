## 1. TDD contract and repository default

- [x] 1.1 Deliver the Gate the configuration change on evidence and a TDD contract decision by adding the named config contract in frontend/tests/unit/utils/viteConfig.test.ts first; the focused test MUST observe config.test.maxWorkers as undefined before the implementation and therefore fail RED, then pass GREEN with value 6 after the implementation. Verify with the focused Vitest invocation npm --prefix frontend run test -- --run tests/unit/utils/viteConfig.test.ts and retain the RED/GREEN result.
- [x] 1.2 Deliver the Preserve the no-CLI release gate while bounding repository default parallelism decision by setting test.maxWorkers to the numeric value 6 in frontend/vite.config.ts while leaving file parallelism and existing test settings unchanged and adding no minWorkers, pool, isolate, or fileParallelism. Verify the focused config contract is GREEN and review the returned config shape for the forbidden keys.

## 2. Stability and scope validation

- [x] 2.1 Deliver the Default-worker full Vitest is the release acceptance mode contract by running npm --prefix frontend run test -- --run three times without CLI worker flags on the 187-file / 1001-test inventory; each run MUST pass with no classified test timeout, external watchdog event, orphaned worker, or changed inventory. Verify the recorded results against the preserved baseline failure (187 files / 1000 tests, 991 pass, 9 timeouts) and the two explicit maxWorkers=6 passes (1000/1000).
- [x] 2.2 Deliver the Keep diagnostics and low-core behavior fail-closed decision by confirming diagnostic runner files remain unchanged, maxWorkers=2 remains rejected after its 300-second watchdog, maxWorkers=6 remains a parallel upper bound, and no timeout, skip, retry, dependency, SourceStep mock, QueryClient, product, or legacy /studio change enters the diff. Verify npm --prefix frontend run lint, npm --prefix frontend run build, the repository-supported line-limit check, git diff --check, and an exact two-file diff review.
