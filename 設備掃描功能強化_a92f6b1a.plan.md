---
name: 設備掃描功能強化
overview: 重構 DeviceScanner 組件的掃描邏輯，改為掃描設備站號/IP網段而非地址範圍，包含輸入驗證、協議參數驗證、進度顯示、結果過濾、性能優化等功能。
todos:
  - id: scan-logic-rtu
    content: 實現 RTU 掃描邏輯：站號範圍掃描（起始站號-最大站號）、測試位置配置、對每個站號測試固定位置
    status: pending
  - id: scan-logic-modbus
    content: 實現 Modbus TCP/UDP 掃描邏輯：連線後與RTU相同，站號範圍掃描、測試位置配置
    status: pending
  - id: scan-logic-fatek
    content: 實現 FATEK 掃描邏輯：站號範圍掃描、測試位置配置、Symbol配置
    status: pending
  - id: scan-logic-mcprotocol
    content: 實現 MC Protocol 掃描邏輯：IP網段掃描（CIDR格式）、依序測試每個IP是否可連線
    status: pending
  - id: ui-station-config
    content: 添加站號配置UI：起始站號、最大站號、測試位置輸入框（Modbus RTU/TCP/UDP、FATEK）
    status: pending
  - id: ui-ip-network-config
    content: 添加IP網段配置UI：IP網段輸入（CIDR格式，如192.168.1.0/24）、MC Protocol專用
    status: pending
  - id: validation
    content: 實現輸入驗證：站號範圍驗證（1-247 for Modbus, 0-254 for FATEK）、IP網段格式驗證、測試位置驗證
    status: pending
  - id: progress
    content: 添加掃描進度顯示：進度條、百分比、已掃描/總數、預計剩餘時間
    status: pending
  - id: speed-control
    content: 添加掃描速度控制：延遲時間配置、並發掃描選項、並發數量配置
    status: pending
  - id: results-enhancement
    content: 增強掃描結果：顯示站號/IP、過濾功能、排序功能、搜索功能、統計圖表
    status: pending
  - id: error-handling
    content: 改進錯誤處理：友好的錯誤提示、即時驗證反饋、詳細錯誤信息
    status: pending
---

# 設備掃描功能強化計劃

## 目標

重構 `DeviceScanner` 組件的掃描邏輯，從掃描地址範圍改為掃描設備站號/IP網段，提升功能完整性、用戶體驗和性能。

## 核心邏輯變更

### 掃描邏輯重新設計

#### 1. Modbus RTU 掃描邏輯

- **配置項**：
  - 起始站號（1-247）
  - 最大站號（1-247，必須 >= 起始站號）
  - 測試位置（固定地址，用於測試每個站號）
  - 操作類型（read_coils, read_holding_registers 等）
- **掃描流程**：

  1. 從起始站號開始，依序掃描到最大站號
  2. 對每個站號，使用設定的測試位置執行讀取操作
  3. 若設備有回應（成功讀取），則將該站號加入發現的設備列表
  4. 若無回應或錯誤，則跳過該站號

- **文件**：`web/test-ui/src/components/DeviceScanner.tsx`

#### 2. Modbus TCP/UDP 掃描邏輯

- **配置項**：
  - 起始站號（1-247）
  - 最大站號（1-247，必須 >= 起始站號）
  - 測試位置（固定地址）
  - 操作類型
- **掃描流程**：
  - 與 RTU 相同，但需要先建立 TCP/UDP 連線
  - 使用現有的 connectionId 進行掃描
- **文件**：`web/test-ui/src/components/DeviceScanner.tsx`

#### 3. FATEK 掃描邏輯

- **配置項**：
  - 起始站號（0-254，FATEK 站號範圍）
  - 最大站號（0-254，必須 >= 起始站號）
  - 測試位置（固定地址）
  - Symbol（組件符號：D, M, X, Y 等）
  - 操作類型（read_registers, read_status）
- **掃描流程**：

  1. 從起始站號開始，依序掃描到最大站號
  2. 對每個站號，使用設定的 Symbol 和測試位置執行讀取操作
  3. 若設備有回應，則將該站號加入發現的設備列表

- **文件**：`web/test-ui/src/components/DeviceScanner.tsx`

#### 4. MC Protocol 掃描邏輯

- **配置項**：
  - IP 網段（CIDR 格式，如 192.168.1.0/24）
  - 埠號（預設 5000 或 5002）
  - 超時時間
- **掃描流程**：

  1. 解析 IP 網段，生成所有 IP 地址列表
  2. 依序對每個 IP 發送連線測試（使用 check_connection 或輕量級讀取）
  3. 若 IP 可連線，則將該 IP 加入發現的設備列表
  4. 顯示每個 IP 的連線狀態

- **文件**：`web/test-ui/src/components/DeviceScanner.tsx`

### 1. 輸入驗證與錯誤處理

#### 1.1 站號範圍驗證

- **Modbus**：站號範圍 1-247
- **FATEK**：站號範圍 0-254
- 起始站號必須 <= 最大站號
- 總掃描次數計算與警告（超過 247 次時提示）
- 文件：`web/test-ui/src/components/DeviceScanner.tsx`

#### 1.2 測試位置驗證

- 測試位置必須 >= 0
- 根據協議和操作類型驗證位置範圍
- 文件：`web/test-ui/src/components/DeviceScanner.tsx`

#### 1.3 IP 網段驗證（MC Protocol）

- 驗證 CIDR 格式（如 192.168.1.0/24）
- 驗證網段範圍（避免掃描過大網段，如 /16）
- 計算網段內 IP 數量並警告（超過 256 個 IP 時提示）
- 文件：`web/test-ui/src/components/DeviceScanner.tsx`

#### 1.4 協議特定參數驗證

- **FATEK Symbol 驗證**
  - 將 Symbol 改為下拉選擇框
  - 支援的 Symbol：X, Y, M, S, T, C, R, D, RT, RC, DR, DW, DWM
  - 根據操作類型自動過濾可用 Symbol
- **操作類型驗證**
  - 確保操作類型已選擇
  - 確保操作類型與協議匹配
- 文件：`web/test-ui/src/components/DeviceScanner.tsx`

### 2. 用戶界面改進

#### 2.1 配置界面重構

- **站號掃描配置**（Modbus RTU/TCP/UDP、FATEK）
  - 起始站號輸入框
  - 最大站號輸入框
  - 測試位置輸入框
  - 操作類型下拉選擇
  - Symbol 下拉選擇（FATEK）
- **IP 網段掃描配置**（MC Protocol）
  - IP 網段輸入框（CIDR 格式）
  - 埠號輸入框
  - 超時時間配置
- 文件：`web/test-ui/src/components/DeviceScanner.tsx`

#### 2.2 掃描進度顯示

- 添加進度條組件（使用 Tailwind CSS）
- 顯示當前進度百分比
- 顯示已掃描/總數（站號或 IP）
- 顯示當前掃描的站號/IP
- 顯示預計剩餘時間
- 文件：`web/test-ui/src/components/DeviceScanner.tsx`

#### 2.3 掃描速度控制

- 添加掃描延遲時間配置（預設 50ms，範圍 0-1000ms）
- 添加並發掃描選項（可選，預設關閉）
- 並發數量配置（1-10，預設 3）
- MC Protocol IP 掃描建議使用較高並發數
- 文件：`web/test-ui/src/components/DeviceScanner.tsx`

#### 2.4 掃描結果增強

- **結果顯示**：
  - 顯示站號（Modbus、FATEK）或 IP 地址（MC Protocol）
  - 顯示測試位置和操作類型
  - 顯示響應數據（如果成功）
  - 顯示錯誤信息（如果失敗）
- **結果操作**：
  - 過濾功能（成功/失敗/全部）
  - 排序功能（按站號/IP 升序/降序）
  - 搜索功能（按站號/IP 搜索）
  - 統計圖表（成功/失敗比例、發現設備數量）
- 文件：`web/test-ui/src/components/DeviceScanner.tsx`

#### 2.5 錯誤提示改進

- 使用更友好的錯誤提示（替換 alert，使用 toast 或內聯提示）
- 添加輸入驗證的即時反饋
- 顯示詳細的錯誤信息（站號/IP、錯誤原因）
- 文件：`web/test-ui/src/components/DeviceScanner.tsx`

### 3. 性能優化

#### 3.1 並發掃描支持

- 實現並發掃描邏輯（使用 Promise.all 和批次處理）
- 可配置並發數量
- 確保掃描結果順序正確（按站號/IP 排序）
- MC Protocol IP 掃描特別適合並發（建議 5-10 並發）
- 文件：`web/test-ui/src/components/DeviceScanner.tsx`

#### 3.2 掃描結果優化

- 使用虛擬滾動（如果結果超過 100 筆）
- 優化大結果集的渲染性能
- 實時更新結果列表（不阻塞 UI）
- 文件：`web/test-ui/src/components/DeviceScanner.tsx`

#### 3.3 掃描統計增強

- 添加平均響應時間統計
- 添加成功率統計（發現設備數/總掃描數）
- 添加掃描速度統計（站號/秒 或 IP/秒）
- 顯示發現的設備總數
- 文件：`web/test-ui/src/components/DeviceScanner.tsx`

### 4. API 整合

#### 4.1 讀取 API 調用

- Modbus RTU/TCP/UDP：使用現有 read API，動態設置 unit_id/station
- FATEK：使用現有 read API，動態設置 station
- MC Protocol：需要新的連線測試 API 或使用輕量級讀取
- 文件：`web/test-ui/src/components/DeviceScanner.tsx`、`web/test-ui/src/services/api.ts`

#### 4.2 MC Protocol 連線測試

- 可能需要新增 API 端點用於快速測試 IP 連線
- 或使用現有 read API 進行輕量級測試（讀取少量數據）
- 文件：`internal/api/handlers/test.go`（如需要）

## 實現細節

### 新增類型定義

在 `web/test-ui/src/components/DeviceScanner.tsx` 中添加：

```typescript
// 掃描結果介面
interface ScanResult {
  stationOrIp: number | string; // 站號或 IP 地址
  success: boolean;
  data?: any;
  error?: string;
  responseTime?: number; // 響應時間（毫秒）
}

// 掃描配置介面
interface ScanConfig {
  // 站號掃描配置（Modbus、FATEK）
  startStation?: number;
  maxStation?: number;
  testAddress?: number;
  operation?: string;
  symbol?: string; // FATEK
  
  // IP 網段掃描配置（MC Protocol）
  ipNetwork?: string; // CIDR 格式
  port?: number;
  timeout?: number;
  
  // 通用配置
  scanDelay?: number;
  concurrent?: boolean;
  concurrency?: number;
}

// 掃描統計介面
interface ScanStatistics {
  totalScanned: number;
  foundDevices: number;
  failedScans: number;
  averageResponseTime: number;
  scanSpeed: number; // 站號/秒 或 IP/秒
}
```

### 新增常數

- Modbus 站號範圍：1-247
- FATEK 站號範圍：0-254
- FATEK Symbol 列表
- 預設配置模板

### 新增狀態

- `startStation`: 起始站號（Modbus、FATEK）
- `maxStation`: 最大站號（Modbus、FATEK）
- `testAddress`: 測試位置
- `ipNetwork`: IP 網段（MC Protocol，CIDR 格式）
- `scanDelay`: 掃描延遲時間
- `concurrentScan`: 是否啟用並發掃描
- `concurrency`: 並發數量
- `filterType`: 結果過濾類型
- `sortOrder`: 排序順序
- `searchTerm`: 搜索關鍵字
- `scanStatistics`: 掃描統計信息
- `currentScanning`: 當前正在掃描的站號/IP

### 新增函數

- `validateInputs()`: 輸入驗證（站號範圍、IP 網段格式等）
- `parseCIDR()`: 解析 CIDR 格式，生成 IP 列表
- `scanModbusStation()`: 掃描單個 Modbus 站號
- `scanFatekStation()`: 掃描單個 FATEK 站號
- `scanMCProtocolIP()`: 掃描單個 MC Protocol IP
- `scanWithConcurrency()`: 並發掃描實現
- `filterResults()`: 結果過濾
- `sortResults()`: 結果排序
- `calculateStatistics()`: 計算統計信息

## 文件修改清單

1. `web/test-ui/src/components/DeviceScanner.tsx` - 主要修改文件

   - 重構掃描邏輯（從地址掃描改為站號/IP 掃描）
   - 添加站號配置 UI（Modbus、FATEK）
   - 添加 IP 網段配置 UI（MC Protocol）
   - 添加輸入驗證邏輯
   - 添加進度條組件
   - 添加結果過濾和排序
   - 添加並發掃描支持
   - 改進錯誤處理

2. `web/test-ui/src/services/api.ts` - 可能需要修改

   - 檢查 read API 是否支持動態設置站號
   - 如需要，添加 MC Protocol 連線測試 API

3. `internal/api/handlers/test.go` - 可能需要修改（如需要新 API）

   - 添加 MC Protocol 快速連線測試端點（可選）

## 掃描邏輯流程圖

### Modbus RTU/TCP/UDP 掃描流程

```
開始
  ↓
輸入：起始站號、最大站號、測試位置、操作類型
  ↓
驗證輸入
  ↓
從起始站號開始循環
  ↓
對當前站號執行讀取操作（使用測試位置）
  ↓
有回應？
  ├─ 是 → 加入發現設備列表
  └─ 否 → 跳過
  ↓
下一個站號
  ↓
是否達到最大站號？
  ├─ 否 → 繼續循環
  └─ 是 → 完成，顯示結果
```

### FATEK 掃描流程

```
開始
  ↓
輸入：起始站號、最大站號、測試位置、Symbol、操作類型
  ↓
驗證輸入
  ↓
從起始站號開始循環
  ↓
對當前站號執行讀取操作（使用 Symbol 和測試位置）
  ↓
有回應？
  ├─ 是 → 加入發現設備列表
  └─ 否 → 跳過
  ↓
下一個站號
  ↓
是否達到最大站號？
  ├─ 否 → 繼續循環
  └─ 是 → 完成，顯示結果
```

### MC Protocol 掃描流程

```
開始
  ↓
輸入：IP 網段（CIDR）、埠號、超時時間
  ↓
解析 CIDR，生成 IP 列表
  ↓
依序對每個 IP 執行連線測試
  ↓
可連線？
  ├─ 是 → 加入發現設備列表
  └─ 否 → 跳過
  ↓
下一個 IP
  ↓
是否掃描完所有 IP？
  ├─ 否 → 繼續循環
  └─ 是 → 完成，顯示結果
```

## 測試要點

1. 輸入驗證測試

   - 測試無效的站號範圍（Modbus 1-247，FATEK 0-254）
   - 測試無效的 IP 網段格式
   - 測試測試位置範圍
   - 測試操作類型與協議的匹配

2. 掃描功能測試

   - 測試 Modbus RTU 站號掃描
   - 測試 Modbus TCP/UDP 站號掃描
   - 測試 FATEK 站號掃描
   - 測試 MC Protocol IP 網段掃描
   - 測試並發掃描
   - 測試掃描中斷

3. 結果處理測試

   - 測試結果過濾（成功/失敗/全部）
   - 測試結果排序（按站號/IP）
   - 測試結果搜索
   - 測試統計信息顯示

4. 邊界情況測試

   - 測試無設備回應的情況
   - 測試部分設備回應的情況
   - 測試大範圍掃描（247 個站號或 256 個 IP）
   - 測試並發掃描的穩定性