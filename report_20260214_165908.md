# P3 完成後 Core Review 改善報告

## 已完成修正

### 1) UDP Transport 逾時設定失敗時清理連線
- **檔案**: `internal/protocol/modbus/transport_udp.go`
- **優先級**: P1
- **改善內容**:
  - 在 `SetDeadline` 失敗時呼叫 `internalClose()` 釋放連線。
  - 新增 `internalClose` 以統一 UDP 傳輸層錯誤收斂行為。
- **預期效果**:
  - 與 TCP/RTU 錯誤處理策略一致。
  - 降低異常狀態下連線殘留與後續通訊不穩定風險。

## 本輪第 3 批 lint 清理（已完成）

### 2) 補齊型別斷言與寫入錯誤檢查
- **檔案**:
  - `internal/virtual/server/modbus/server.go`
  - `internal/virtual/server/modbus/server_connection.go`
  - `lib/hsllogic/packet_logger.go`
  - `lib/hsllogic/packet_logger_types.go`
- **優先級**: P1
- **改善內容**:
  - 檢查 listener address type assertion。
  - 檢查 `conn.Write` / `Sync` 結果。
  - 修正 object pool type assertion 檢查。
- **預期效果**:
  - 降低 runtime panic 機率。
  - 提升 I/O 失敗時可預期行為。

## 後續建議（未完成）

### A) 持續清理既有 errcheck 遺留
- **範圍**: `internal/api/handlers/*`, `internal/datalink/*/sql_repo.go`, `internal/protocol/fatek`, `internal/protocol/mcprotocol`, `internal/web/embed.go` 等
- **優先級**: P2
- **預期效果**: 提升整體錯誤可觀測性，降低靜默失敗風險。
