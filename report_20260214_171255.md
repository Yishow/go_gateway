# Core Review 改善任務報告

## 範圍
- 檢視 commit: `4da9195`、`032b0e2`
- 針對 reviewer 指出的 correctness 與型別轉換風險進行修正

## 改善任務

### 1) 修正暫存器位址溢出風險
- 優先級：P0（Critical）
- 檔案：`internal/datalink/modbusshare/service_write.go`
- 問題：多字值寫入末端暫存器時可能發生位址回繞。
- 措施：改為整數位址計算並加入 `baseRegister+len(words) > 65536` 邊界檢查。
- 預期效果：避免跨字寫入到錯誤暫存器，確保寫入一致性與資料正確性。
- 狀態：✅ 已完成

### 2) 修正負數轉 uint64 靜默回繞
- 優先級：P1（High）
- 檔案：`internal/datalink/modbusshare/service_encoding.go`
- 問題：負數有號整數轉 `uint64` 會靜默回繞成極大值。
- 措施：對 `int/int8/int16/int32/int64` 增加負值檢查並回傳錯誤。
- 預期效果：避免資料污染與難以追蹤的數值異常。
- 狀態：✅ 已完成

### 3) 修正 uint64 轉 int64 溢出
- 優先級：P1（High）
- 檔案：`internal/datalink/modbusshare/service_encoding.go`
- 問題：超過 `math.MaxInt64` 的 `uint64` 轉 `int64` 會溢出為負值。
- 措施：加入 `uint` / `uint64` 範圍檢查；浮點轉整數也補齊上下界檢查。
- 預期效果：避免整數溢出導致的錯誤鏡像值寫入。
- 狀態：✅ 已完成

## 測試驗證
- 新增測試：
  - `TestToUint64_NegativeSignedValue_ReturnsError`
  - `TestToInt64_Uint64Overflow_ReturnsError`
  - `TestService_WriteTagValue_RegisterOverflow_ReturnsError`
- 執行結果：
  - `go test ./internal/datalink/modbusshare -run 'TestToUint64_NegativeSignedValue_ReturnsError|TestToInt64_Uint64Overflow_ReturnsError|TestService_WriteTagValue_RegisterOverflow_ReturnsError|TestService_WriteTagValue_Float32AndInt16|TestService_Status_AfterStartAndStop|TestService_Start_PortConflict_ReturnsActionableError|TestService_ModbusClientRead_MirroredValues|TestService_RemoveMappingAndHasMapping'` ✅
  - `go test ./...` ✅
