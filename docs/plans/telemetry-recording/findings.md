# 規畫結論

核心缺口不是多一個欄位，而是缺少「來源數值的意義」與「記錄計畫」的分層。
一段混合PLC範圍要先逐項解碼，再按用途記錄；不是將溫度、壓力與累積值整段套平均或差分。
managed長表提供一致身份與品質，進階wide/grouped繼續服務既有表；兩者共用計量結果。
已接受記錄的存活權獨立於目前mapping enable，避免disable後丟掉斷線期間的資料。
官方CLI未安裝，嘗試npm registry因DNS EAI_AGAIN失敗；不能把自訂離線檢查冒充官方驗證。
