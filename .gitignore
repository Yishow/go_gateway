# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
.gocache
.gotmp
graphify-out/
# cmd/test_ui/static 為前端 build 產物（scripts/build.ps1 / start.ps1 會從 frontend/dist 同步）
# 僅追蹤佔位檔，確保 fresh clone 後 go:embed static 可編譯
cmd/test_ui/static/*
!cmd/test_ui/static/embed-placeholder.txt

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories
vendor/

# Go workspace file
go.work

# Build output directories
bin/
dist/
build/
out/

# IDE and Editor files
.idea/
*.swp
*.swo
*~
.DS_Store

# VS Code - keep project settings, ignore user-specific
# Note: .vscode/launch.json and settings.json are kept for project consistency

# Environment files
.env
.env.local
.env.*.local

# Log files
*.log

# Temporary files
*.tmp
*.temp
.tmp/
.temp/

# OS generated files
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
.Spotlight-V100
.Trashes

# Frontend build output and dependencies
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/.vitest/

# Legacy frontend directory (if still exists)
web/test-ui/node_modules/
web/test-ui/dist/
web/test-ui/.vite/
web/test-ui/.vitest/

# Project specific
fatek_test.exe
.temp/

# PDF files (keep docs PDFs if needed, but ignore others)
*.pdf
!docs/*.pdf

# Debug files
debug
__debug_bin

# Coverage reports
coverage.html
coverage.xml
coverage.txt

# Delve (debugger) output
__debug_bin

# Local configuration files
config.local.yaml
config.local.yml
*.local

# Backup files
*.bak
*.backup
*~

.claude\settings.json
datalink.db
datalink.db-shm
datalink.db-wal
gateway.db
gateway.db*

# Git worktrees
.worktrees/
.spectra/
openspec/.vector-search.db*

# MemPalace per-project files (issue #185)
mempalace.yaml
entities.json
