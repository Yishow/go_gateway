---
name: 協議測試前端開發計劃
overview: 建立一個獨立的協議測試前端工具，使用 Vite + React + TypeScript 開發，透過 Go embed 打包成單一執行檔，提供完整的協議測試與工程現場 debug 功能。
todos: []
---

# 協議測試前端開發計劃

## 架構概述

### 技術棧

- **前端**: Vite + React + TypeScript + Tailwind CSS
- **後端**: Go Gin 框架提供 REST API
- **打包**: Go `embed` 將前端靜態資源嵌入執行檔
- **即時通訊**: WebSocket 用於即時數據推送

### 專案結構

```
go-gateway/
├── cmd/
│   └── test_ui/              # 測試工具主程式
│       └── main.go
├── internal/
│   ├── api/
│   │   ├── router.go         # Gin 路由設定
│   │   └── handlers/
│   │       ├── protocol.go   # 協議測試 API
│   │       ├── debug.go      # Debug 功能 API
│   │       └── websocket.go  # WebSocket 處理
│   └── web/
│       └── embed.go          # 前端靜態資源嵌入
├── web/
│   └── test-ui/              # React 前端專案
│       ├── vite.config.ts
│       ├── package.json
│       ├── src/
│       │   ├── App.tsx
│       │   ├── main.tsx
│       │   ├── components/   # UI 組件
│       │   ├── pages/        # 頁面組件
│       │   ├── hooks/        # React Hooks
│       │   ├── services/     # API 服務
│       │   └── types/        # TypeScript 類型定義
│       └── dist/              # 建置輸出（會被 embed）
└── Makefile                   # 建置腳本
```

## 核心功能模組

### 1. 協議選擇與配置

- **協議選擇器**: 快速切換 Modbus/FATEK/MC Protocol
- **連線模式選擇**: TCP/UDP/Serial (RTU)
- **動態配置表單**: 根據選擇的協議和模式顯示對應的配置欄位
- **配置預設值管理**: 保存和載入常用配置

### 2. 測試操作介面

- **讀取操作**: 支援各協議的讀取功能
  - Modbus: Coils, Discrete Inputs, Holding Registers, Input Registers
  - FATEK: Registers, Status, Random Read
  - MC Protocol: Batch Read Word/Bit, Random Read Word
- **寫入操作**: 支援各協議的寫入功能
- **批量測試**: 一次測試多個位址範圍
- **測試腳本**: 定義多步驟測試序列

### 3. Debug 功能

- **原始數據包顯示**: 十六進制格式，可複製
- **請求/回應對比**: 並排顯示，標示差異
- **時間戳記錄**: 精確到毫秒
- **錯誤堆疊追蹤**: 完整的錯誤訊息和堆疊
- **連線狀態日誌**: 連線建立、斷開、重連事件
- **性能指標**: 響應時間、吞吐量統計
- **協議分析器**: 自動解析數據包結構
- **十六進制編輯器**: 手動構建和發送原始數據包

### 4. 監控與可視化

- **監控模式**: 持續讀取並即時顯示（類似示波器）
- **數據圖表**: 使用 Chart.js 或 Recharts 顯示趨勢
- **數據表格**: 即時更新的數據表格
- **對比模式**: 同時連接兩個設備進行數據對比

### 5. 測試管理

- **測試模板**: 保存常用測試配置，快速載入
- **測試結果導出**: CSV/JSON/Excel 格式
- **測試歷史**: 保存測試記錄
- **自動重試**: 失敗時自動重試機制

### 6. 連線管理

- **連線池狀態**: 顯示當前活躍連線
- **連線測試**: 快速測試連線是否正常
- **自動重連**: 斷線自動重連機制

## 後端 API 設計

### 協議測試 API (`/api/v1/test`)

#### 連線管理

- `POST /api/v1/test/connect` - 建立連線
- `POST /api/v1/test/disconnect` - 斷開連線
- `GET /api/v1/test/status` - 取得連線狀態

#### 讀取操作

- `POST /api/v1/test/read` - 執行讀取操作
  ```json
  {
    "protocol": "modbus_tcp",
    "config": { "host": "192.168.1.100", "port": 502, "unitID": 1 },
    "operation": "read_holding_registers",
    "address": 0,
    "count": 10
  }
  ```

#### 寫入操作

- `POST /api/v1/test/write` - 執行寫入操作

#### 批量測試

- `POST /api/v1/test/batch` - 批量測試多個位址

#### 測試腳本

- `POST /api/v1/test/script` - 執行測試腳本
- `GET /api/v1/test/scripts` - 取得測試腳本列表
- `POST /api/v1/test/scripts` - 保存測試腳本

#### 監控模式

- `POST /api/v1/test/monitor/start` - 啟動監控模式
- `POST /api/v1/test/monitor/stop` - 停止監控模式
- `WS /api/v1/test/monitor/stream` - 監控數據流（WebSocket）

### Debug API (`/api/v1/debug`)

- `GET /api/v1/debug/packets` - 取得數據包記錄
- `GET /api/v1/debug/logs` - 取得日誌記錄
- `POST /api/v1/debug/send-raw` - 發送原始數據包
- `GET /api/v1/debug/analyze/:packetId` - 分析數據包結構

### 配置管理 API (`/api/v1/config`)

- `GET /api/v1/config/presets` - 取得配置預設值列表
- `POST /api/v1/config/presets` - 保存配置預設值
- `DELETE /api/v1/config/presets/:id` - 刪除配置預設值

### 測試模板 API (`/api/v1/templates`)

- `GET /api/v1/templates` - 取得測試模板列表
- `POST /api/v1/templates` - 保存測試模板
- `DELETE /api/v1/templates/:id` - 刪除測試模板

### 連線池 API (`/api/v1/connections`)

- `GET /api/v1/connections` - 取得連線池狀態
- `GET /api/v1/connections/:id` - 取得特定連線詳情

## 前端頁面結構

### 主頁面 (`/`)

- **頂部導航**: 協議選擇器、連線狀態指示器
- **左側面板**: 配置表單（根據協議動態顯示）
- **中間區域**: 測試操作區（讀取/寫入/批量/腳本）
- **右側面板**: Debug 面板（數據包、日誌、性能指標）
- **底部**: 監控模式控制、數據圖表

### 子頁面

- `/templates` - 測試模板管理
- `/history` - 測試歷史記錄
- `/compare` - 對比模式
- `/analyzer` - 協議分析器

## 實作步驟

### 階段 1: 專案初始化

1. 建立 `cmd/test_ui/main.go` - 主程式入口
2. 初始化 React + Vite 專案在 `web/test-ui/`
3. 設定 TypeScript、Tailwind CSS
4. 建立 Go embed 機制 (`internal/web/embed.go`)

### 階段 2: 後端 API 基礎架構

1. 建立 Gin 路由 (`internal/api/router.go`)
2. 實作協議測試 API handlers
3. 實作 WebSocket 處理
4. 實作配置管理 API

### 階段 3: 前端基礎架構

1. 建立頁面路由（React Router）
2. 建立 API 服務層（Axios/TanStack Query）
3. 建立 TypeScript 類型定義
4. 建立基礎 UI 組件庫

### 階段 4: 核心功能實作

1. 協議選擇與配置表單
2. 讀取/寫入操作介面
3. Debug 面板（數據包顯示、日誌）
4. 連線管理功能

### 階段 5: 進階功能

1. 批量測試功能
2. 測試腳本功能
3. 監控模式與數據可視化
4. 測試模板管理
5. 協議分析器
6. 十六進制編輯器

### 階段 6: 整合與優化

1. 前端建置與 Go embed 整合
2. 單一執行檔打包測試
3. 跨平台編譯測試
4. 性能優化

## 關鍵檔案

1. `cmd/test_ui/main.go` - 主程式，啟動 HTTP 伺服器
2. `internal/api/router.go` - API 路由設定
3. `internal/api/handlers/protocol.go` - 協議測試處理邏輯
4. `internal/web/embed.go` - 前端靜態資源嵌入
5. `web/test-ui/src/App.tsx` - React 主應用
6. `web/test-ui/src/pages/TestPage.tsx` - 主測試頁面
7. `web/test-ui/vite.config.ts` - Vite 配置（base path 設定）

## 打包流程

1. 前端建置: `cd web/test-ui && npm run build`
2. 輸出到 `web/test-ui/dist/`
3. Go embed 讀取 `dist/` 目錄
4. 編譯: `go build -o test-ui.exe ./cmd/test_ui`
5. 單一執行檔包含所有前端資源

## 注意事項

- Vite 建置時需設定正確的 `base` path（如果部署在子路徑）
- Go embed 需要處理 SPA 路由（所有路徑回傳 index.html）
- WebSocket 連線需要處理跨域問題
- 串列埠操作需要適當的權限處理（Windows/Linux）
- 大量數據包記錄需要考慮記憶體限制
